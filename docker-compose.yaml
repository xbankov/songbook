services:
#   frontend:
#     build:
#       context: ./frontend
#     ports:
#       - "8080:8080"
#     depends_on:
#       - backend

  backend:
    build:
      context: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./app/data:/app/data

